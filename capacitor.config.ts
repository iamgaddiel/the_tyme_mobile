import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'the_tyme_mobile',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
